# CDK Examples - Reference Only

This folder contains CDK example code for comparison purposes during migration.

**Note**: This is reference code only. To use:

```bash
npm install
```

This will install the required CDK dependencies and resolve TypeScript errors.

The examples are provided to help understand the CDK-to-Terraform mapping during migration.
